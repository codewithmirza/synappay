{
  "name": "@synappay/blockchain",
  "version": "0.1.0",
  "description": "SynapPay blockchain interactions and smart contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "deploy:ethereum": "node scripts/deploy-ethereum.js",
    "deploy:stellar": "stellar contract deploy --wasm target/wasm32-unknown-unknown/release/synappay_stellar_htlc.wasm",
    "build:stellar": "cd contracts/stellar && stellar contract build"
  },
  "dependencies": {
    "ethers": "^6.8.0",
    "stellar-sdk": "^11.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0"
  }
}